import React from "react";
import './Header.scss'

function Header(props) {
    return (
        <header className={`landing-header`}>
            <div>
                <h2>HACKER NEWS</h2>
            </div>
        </header>
    );
}

export default Header;